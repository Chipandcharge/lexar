import React from 'react';
import Head from '../components/Head'

const Connect = (props) => {
    return  (
        <> 
            <Head title="Connect" />
            <div>Connect</div>
        </>
    )
}

export default Connect